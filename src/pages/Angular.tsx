import React from "react";

import { Repositories } from "../components";

import angularLogo from "../assets/angular.svg";

const AngularRepos = () => (
  <Repositories term="Angular" techLogo={angularLogo} />
);

export default AngularRepos;
